name: CI

on:
  push:
    tags:
      - "v*"
  pull_request:
    branches: [main]

# Read-only: CI only tests and builds snapshot artifacts. Release publishing
# (which needs contents: write) lives in release.yml and runs on tags only.
permissions:
  contents: read

# The axiom-studio org Actions policy only allows actions from org-owned
# repositories, so these workflows use no `uses:` steps. Checkout, Go
# toolchain setup, and GoReleaser all run as plain shell commands.

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git init -q .
          git remote add origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git fetch -q --depth 1 origin "$GITHUB_SHA"
          git checkout -q --detach FETCH_HEAD

      - name: Set up Go
        run: |
          GO_VERSION="$(awk '$1 == "go" {print $2}' go.mod)"
          curl -fsSL "https://go.dev/dl/go${GO_VERSION}.linux-amd64.tar.gz" -o /tmp/go.tgz
          sudo rm -rf /usr/local/go
          sudo tar -C /usr/local -xzf /tmp/go.tgz
          echo "/usr/local/go/bin" >> "$GITHUB_PATH"
          echo "$HOME/go/bin" >> "$GITHUB_PATH"
          /usr/local/go/bin/go version

      - name: Vet
        run: go vet ./...

      - name: Test
        run: go test ./...

  build:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout (full history + tags)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git init -q .
          git remote add origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git fetch -q --prune --tags origin "$GITHUB_SHA"
          git checkout -q --detach FETCH_HEAD

      - name: Set up Go
        run: |
          GO_VERSION="$(awk '$1 == "go" {print $2}' go.mod)"
          curl -fsSL "https://go.dev/dl/go${GO_VERSION}.linux-amd64.tar.gz" -o /tmp/go.tgz
          sudo rm -rf /usr/local/go
          sudo tar -C /usr/local -xzf /tmp/go.tgz
          echo "/usr/local/go/bin" >> "$GITHUB_PATH"
          echo "$HOME/go/bin" >> "$GITHUB_PATH"
          /usr/local/go/bin/go version

      - name: Install GoReleaser
        run: go install github.com/goreleaser/goreleaser/v2@latest

      # Snapshot build validates the GoReleaser config and cross-compilation.
      # Artifacts are not uploaded: actions/upload-artifact is blocked by the
      # org Actions policy and there is no action-free equivalent for workflow
      # artifacts. Release builds publish their assets via release.yml.
      - name: Build snapshot
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: goreleaser release --snapshot --clean
